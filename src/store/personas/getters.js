export function getPersonas(state) {
  return state.getPersonas;
}

export function getPersonasFilter(state) {
  return state.getPersonasFilter;
}

export function callAddPersonas(state) {
  return state.getPersonas;
}
